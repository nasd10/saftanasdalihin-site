import { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: '*',
        allow: '/',
        // Disallow: '/private',
      },
    ],
    sitemap: 'https://saftanasdalihin-site.vercel.app/sitemap.xml',
  };
}